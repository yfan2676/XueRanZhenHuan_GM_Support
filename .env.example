# 复制为 .env 后填写（.env 已在 .gitignore 中，不会提交）
#
# 入场口令：设置后所有页面与接口都需先输口令；留空或不设则完全关闭校验（局域网自用）。
# 口令可以是任意字符串。公网暴露时建议用随机串而非好记的词：
#   python3 -c "import secrets; print(secrets.token_urlsafe(24))"
GM_PASSWORD=
